import {
  createContext,
  useCallback,
  useContext,
  useRef,
  useState,
} from 'react';
import { createPortal } from 'react-dom';

/**
 * Placeholder ref
 */

export function useInversePortal() {
  return {};
}
